// Code generated by c2plan9. DO NOT EDIT.

//go:build amd64 && !purego

#include "textflag.h"

// func json_skip_whitespace_avx2
TEXT ·json_skip_whitespace_avx2(SB), NOSPLIT, $0-56
	MOVQ arg0+0(FP), DI
	MOVQ arg1+8(FP), SI
	MOVQ arg2+16(FP), DX
	MOVQ arg3+24(FP), CX
	MOVQ arg4+32(FP), R8
	MOVQ arg5+40(FP), R9

	MOVQ SI, AX
	MOVL $0x20202020, CX
	VMOVD CX, X0
	VPBROADCASTD X0, X0
	MOVL $0x9090909, CX
	VMOVD CX, X1
	VPBROADCASTD X1, X1
	MOVL $0xd0d0d0d, CX
	VMOVD CX, X2
	VPBROADCASTD X2, X2
	MOVL $0xa0a0a0a, CX
	VMOVD CX, X3
	VPBROADCASTD X3, X3
	VINSERTI128 $0x1, X0, Y0, Y0
	VINSERTI128 $0x1, X1, Y1, Y1
	VINSERTI128 $0x1, X2, Y2, Y2
	VINSERTI128 $0x1, X3, Y3, Y3
	NOP
L_0060:
	LEAQ 0x20(DX), CX
	CMPQ CX, AX
	JA L_00a7
	VMOVDQU 0(DI)(DX*1), Y4
	VPCMPEQB Y0, Y4, Y5
	VPCMPEQB Y1, Y4, Y6
	VPOR Y6, Y5, Y5
	VPCMPEQB Y2, Y4, Y6
	VPCMPEQB Y3, Y4, Y4
	VPOR Y4, Y6, Y4
	VPOR Y4, Y5, Y4
	VPMOVMSKB Y4, SI
	MOVQ CX, DX
	CMPL SI, $-0x1
	JE L_0060
	NOTL SI
	TZCNTL SI, AX
	ADDQ CX, AX
	ADDQ $-0x20, AX
L_00a3:
	VZEROUPPER
	MOVQ AX, ret+48(FP)
	RET
L_00a7:
	CMPQ DX, AX
	JAE L_00a3
	MOVQ $0x100002600, SI
	NOP
L_00c0:
	MOVBLZX 0(DI)(DX*1), R8
	CMPQ R8, $0x20
	JA L_00ea
	BTQ R8, SI
	JAE L_00ea
	CMPL R8, $0x20
	JA L_00f3
L_00d7:
	MOVL R8, R8
	BTQ R8, SI
	JAE L_00f3
	INCQ DX
	CMPQ DX, AX
	JB L_00c0
	JMP L_00a3
L_00ea:
	MOVQ DX, CX
	CMPL R8, $0x20
	JBE L_00d7
L_00f3:
	MOVQ CX, AX
	VZEROUPPER
	MOVQ AX, ret+48(FP)
	RET

// func json_scan_string_avx2
TEXT ·json_scan_string_avx2(SB), NOSPLIT, $0-56
	MOVQ arg0+0(FP), DI
	MOVQ arg1+8(FP), SI
	MOVQ arg2+16(FP), DX
	MOVQ arg3+24(FP), CX
	MOVQ arg4+32(FP), R8
	MOVQ arg5+40(FP), R9

	MOVL $0x22222222, AX
	VMOVD AX, X0
	VPBROADCASTD X0, X0
	MOVL $0x5c5c5c5c, AX
	VMOVD AX, X1
	VPBROADCASTD X1, X1
	VINSERTI128 $0x1, X0, Y0, Y0
	VINSERTI128 $0x1, X1, Y1, Y1
	NOP
L_0030:
	LEAQ 0x20(DX), AX
	CMPQ AX, SI
	JA L_0064
	VMOVDQU 0(DI)(DX*1), Y2
	VPCMPEQB Y0, Y2, Y3
	VPCMPEQB Y1, Y2, Y2
	VPOR Y2, Y3, Y2
	VPMOVMSKB Y2, CX
	MOVQ AX, DX
	TESTL CX, CX
	JE L_0030
	TZCNTL CX, CX
	ADDQ CX, AX
	ADDQ $-0x20, AX
	VZEROUPPER
	MOVQ AX, ret+48(FP)
	RET
L_0064:
	MOVQ $-0x1, AX
	CMPQ DX, SI
	JAE L_00a1
L_0070:
	MOVBLZX 0(DI)(DX*1), R9
	MOVQ DX, R8
	CMPL R9, $0x22
	JE L_008a
	MOVQ DX, R8
	CMPL R9, $0x5c
	JE L_008a
	MOVQ CX, R8
L_008a:
	CMPL R9, $0x22
	JE L_00a5
	CMPL R9, $0x5c
	JE L_00a5
	INCQ DX
	MOVQ R8, CX
	CMPQ DX, SI
	JB L_0070
L_00a1:
	VZEROUPPER
	MOVQ AX, ret+48(FP)
	RET
L_00a5:
	MOVQ R8, AX
	VZEROUPPER
	MOVQ AX, ret+48(FP)
	RET

